# Style & Conventions
- Prettier config: semi = true, singleQuote = true, tabWidth = 2, trailingComma = es5, printWidth = 120.
- React codebase uses JSX with Vite; follow existing component patterns in `src/pages` and `src/components`.
